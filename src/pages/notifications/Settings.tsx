import { __ } from '@wordpress/i18n';

const Settings = () => {
  return (
    <p className="text-sm text-muted-foreground">
      {__('Settings content goes here.', 'texty')}
    </p>
  );
};

export default Settings;
