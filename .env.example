WP_BASE_URL=http://dokan.test
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=password

# Optional — live gateway credentials for the connection/send suites
# (rest-gateway-connection.spec.ts). Absent => those tests skip cleanly.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
CLICKATELL_API_KEY=
